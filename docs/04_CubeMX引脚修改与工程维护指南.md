# CubeMX引脚修改与工程维护指�?

## 1. 这份文档为什么重�?

这个工程最大的价值之一，就是“以后还能改”�?

�?STM32 工程最容易被改坏的地方，也正是�?

1. 引脚
2. 外设
3. DMA
4. 中断
5. 生成代码与用户代码的边界

所以本文件专门回答两个问题�?

1. 如果我要改引脚，应该怎么改才不破坏整个工程？
2. 如果我要重新生成 `CubeMX` 代码，应该检查什么？

## 2. 先记住一个核心原�?

本工程改硬件配置时，通常不是只改一个地方，而是要联�?3 层：

1. `CubeMX` 配置�?
2. 软件绑定�?
3. 文档�?

### 2.1 CubeMX 配置�?

就是�?

1. `.ioc`
2. `Core/Inc/main.h`
3. `Core/Src/gpio.c`
4. `Core/Src/usart.c`
5. `Core/Src/tim.c`
6. `Core/Src/adc.c`
7. `Core/Src/i2c.c`

### 2.2 软件绑定�?

主要就是�?

1. `Code/01_Task/Src/task_init.c`
2. `Code/01_Task/Src/task_stepper.c`
3. 某些模块�?`bind` 参数�?`active_level`

### 2.3 文档�?

至少要同步更新：

1. [硬件连接与外设映射总表](05_硬件连接与外设映射总表.md)
2. [系统启动流程与运行逻辑总览](06_系统启动流程与运行逻辑总览.md)
3. 对应层级的开发手�?
4. 必要时更�?`README.md`

## 3. `.ioc` 在本工程里的地位

### 3.1 它是硬件配置源头

在本工程里，真正的“引脚配置源”不�?`main.h`，而是�?

```text
Summer_26_zgt6_freertos.ioc
```

也就是说�?

1. �?GPIO
2. �?UART
3. �?TIM
4. �?ADC
5. �?I2C
6. �?DMA
7. 改中断优先级

都应优先�?`.ioc` 里做�?

### 3.2 `main.h` 只是结果，不是起�?

很多新手直接�?`main.h` 里的 `#define`�?

这在本工程里是不推荐的，因为�?

1. 下次 `CubeMX` 重新生成时可能会被覆�?
2. 这样做容易让 `.ioc` 和生成代码彻底不一�?

## 4. 改引脚的标准流程

### 4.1 第一步：明确修改对象类型

先问自己：我要改的是哪一类东西？

1. 普�?GPIO 输出
2. 普�?GPIO 输入
3. 串口
4. 定时�?PWM
5. 编码器定时器
6. ADC
7. I2C

不同对象改动后需要联动检查的位置不一样�?

### 4.2 第二步：�?`CubeMX` 中修�?`.ioc`

只要涉及外设或引脚本体变化，都应先改 `.ioc`�?

改完后重新生成代码�?

### 4.3 第三步：检�?`Core` 生成结果

重点检查：

1. `Core/Inc/main.h`
2. `Core/Src/gpio.c`
3. `Core/Src/usart.c`
4. `Core/Src/tim.c`
5. `Core/Src/adc.c`
6. `Core/Src/i2c.c`
7. `Core/Src/freertos.c`

### 4.4 第四步：检查分层绑定表

这一步是本工程和普通裸机工程最不同的地方�?

即使 `CubeMX` 已经改对了，仍然必须继续检查：

1. `task_init.c` 中的 `mod_led` 绑定�?
2. `task_init.c` 中的 `mod_relay` 绑定�?
3. `task_init.c` 中的 `mod_key` 绑定�?
4. `task_init.c` 中的 `mod_sensor` 绑定�?
5. `task_init.c` 中的 `mod_motor` 绑定�?
6. `task_init.c` 中的 `mod_battery` 绑定
7. `task_init.c` 中的 `mod_vofa` / `mod_k230` 绑定
8. `task_stepper.c` 中的步进串口绑定

### 4.5 第五步：检查有没有“软件修正�?

本工程有一些地方不是单纯按原理图直连语义来写的，而是做过软件修正�?

例如�?

1. 左电机方向引脚在绑定表里做了纠正
2. 右轮编码器方向做了软件取�?
3. LED 是低电平有效

### 4.6 第六步：重新构建、烧录、联�?

改完以后至少要做�?

1. 重新构建
2. 重新下载
3. 验证该功能对应的链路是否还正�?

### 4.7 第七步：更新文档

最后一步不能省�?

如果修改了引脚但不更新文档，后续维护者将很难判断真实映射关系�?

## 5. 按对象分类的修改方法

### 5.1 �?LED、继电器、普�?GPIO

通常要改�?

1. `.ioc`
2. `main.h`
3. `gpio.c`
4. `task_init.c` 中对应的绑定�?

如果电气有效电平变了，还要改�?

1. `active_level`

### 5.2 改按�?

通常要改�?

1. `.ioc`
2. `main.h`
3. `gpio.c`
4. `task_init.c` �?`mod_key_hw_cfg_t`

如果上下拉方式变了，还要同步确认�?

1. `GPIO_PULLUP / GPIO_PULLDOWN`
2. `active_level`

### 5.3 改灰度传感器

通常要改�?

1. `.ioc`
2. `main.h`
3. `gpio.c`
4. `task_init.c` �?`s_sensor_bind_map`

如果不仅更换了引脚，还调整了传感器物理顺序，那么还要同步修改�?

1. 权重因子 `factor`
2. 文档里的通道顺序

### 5.4 改串�?

串口是最容易“只改了一半”的地方�?

例如�?VOFA �?`USART1` 换到别的串口，那么至少要改：

1. `.ioc` 中该串口和对应引�?
2. `usart.c`
3. `task_init.c` �?`mod_vofa` 绑定
4. 可能还要确认 `drv_uart_get_port_index` 是否支持这个串口实例
5. `docs` 中的串口映射说明

同理�?

1. `mod_k230` 对应 `USART2`
2. `Stepper X` 对应 `USART3`
3. `Stepper Y` 对应 `USART6`

这些改动都不能只在一处完成�?

### 5.5 �?PWM

底盘 PWM 当前使用�?

1. `TIM3_CH1`
2. `TIM3_CH2`

如果修改 PWM 引脚或通道�?

1. 先改 `.ioc`
2. 再看 `tim.c`
3. 再看 `task_init.c` 里的 `mod_motor_hw_cfg_t`
4. 再确�?`pwm_channel`

### 5.6 改编码器

如果修改了编码器对应的定时器或通道�?

1. 先改 `.ioc`
2. 再看 `tim.c`
3. 再看 `task_init.c` 里的 `enc_htim`
4. 再看 `enc_counter_bits`
5. 再看 `enc_invert`

### 5.7 �?ADC

如果修改电池采样引脚�?

1. 先改 `.ioc`
2. 再看 `adc.c`
3. 再看 `task_init.c` �?`mod_battery_bind_t`

如果修改的不是引脚，而是分压比，那么主要不是�?`.ioc`，而是修改�?

1. `voltage_ratio`
2. 相关文档说明

### 5.8 �?I2C �?OLED

如果修改 OLED �?I2C 口：

1. �?`.ioc`
2. �?`i2c.c`
3. �?`task_oled.c` 中绑定的 `hi2c`

## 6. `.ioc` 重新生成代码后要检查什�?

每次重新生成之后，至少检查以下内容：

1. `main.h` 中引脚宏是否符合预期
2. `gpio.c/usart.c/tim.c/adc.c/i2c.c` 是否符合预期
3. `freertos.c` 中任务和同步资源是否没有被意外覆�?
4. `USER CODE` 区域外是否有项目自定义代码被覆盖
5. `Code/` 下手写层是否没有被破�?
6. 构建是否仍然通过

## 7. 本工程为什么“改引脚”不等于“改功能�?

这是本工程的一个核心设计点�?

1. 引脚决定的是硬件连接�?
2. 模块绑定决定的是软件语义�?
3. 任务层决定的是业务行为�?

因此必须区分�?

1. 硬件连接
2. 模块语义
3. 任务行为

## 8. 改动后的最小验证策�?

每次改完引脚或外设后，不要整车全流程跑一遍再找问题�?

建议按“最短验证链”做�?

1. �?LED：只�?LED
2. 改按键：只测按键事件和反�?
3. �?UART：只测该 UART 对应模块
4. �?PWM：只测对应电�?
5. 改编码器：只测编码器读数
6. �?ADC：只测电压采�?
7. �?I2C：只�?OLED

## 9. 一句话总结

在这个工程里，改引脚不是“改一个宏”，而是“改一整条链”：

1. `.ioc`
2. 生成代码
3. 绑定�?
4. 模块语义
5. 联调验证
6. 文档同步

