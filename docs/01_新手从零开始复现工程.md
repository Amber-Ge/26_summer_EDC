# 新手从零开始复现工�?

## 1. 这份文档解决什么问�?

这份文档是给完全新手准备的�?

如果当前处于以下情况�?

1. 还没有搭�?`STM32 + CLion + CubeMX + CMake` 环境�?
2. 不知道这个仓库应该如何打开�?
3. 不知道怎么编译、烧录、调试�?
4. 不知道硬件该怎么连接�?

那么请按本文件顺序一步一步做，不要跳步骤�?

## 2. 最终需要复现到什么状�?

在最基础的可运行状态下，复现者至少应完成下面几件事：

1. 工程能在 `CLion` 或命令行里成功编译�?
2. 生成 `.elf` 文件�?
3. 程序能通过 `ST-LINK` 下载到板子�?
4. 上电�?`FreeRTOS` 能正常启动�?
5. `InitTask` 完成初始化后，各业务任务能进入主循环�?
6. `OLED` 能显示信息�?
7. 按键有反馈，LED/蜂鸣�?激光链路工作正常�?
8. `VOFA`、`K230`、双轴步进、底盘电机这些链路可以按需继续联调�?

## 3. 所需软件环境

建议环境如下�?

| 软件 | 建议说明 | 备注 |
| --- | --- | --- |
| `CLion` | 建议使用较新的版本，例如 `2025.3` | 本工程按 `CLion + CMake` 使用 |
| `STM32CubeMX` | 工程里的 `.ioc` 来自 `6.15.0` | 用于改引脚和重新生成代码 |
| `STM32CubeCLT` | 当前工程调试配置里可�?`1.18.0` | 内含工具链、GDB Server、Programmer |
| `CMake` | 版本不低�?`3.22` | `CMakeLists.txt` 要求 `3.22` |
| `Ninja` | 推荐安装 | `CMakePresets.json` 默认生成器就�?`Ninja` |
| `ST-LINK` 驱动 | 必装 | 用于下载和调�?|
| 串口工具 | 任意 | 用于普通串口联�?|
| `VOFA+` | 可选但强烈建议 | 用于波形观察 |

## 4. 所需硬件

### 4.1 核心硬件

1. `STM32F407ZGT6` 开发板
2. 项目配套扩展 PCB
3. `ST-LINK`
4. 稳定电源

### 4.2 外设硬件

1. 12 路灰度循迹模�?
2. 轮趣 `MG513X` 直流电机及驱动链�?
3. 张大头闭环步进电机驱动一体设�?
4. `K230`
5. OLED
6. USB 转串口工�?

### 4.3 上电前必须知道的硬件注意事项

1. 步进驱动串口通信必须和单片机共地�?
2. LED 是低电平有效，不要以为“高电平亮”�?
3. 左电机方向线在软件映射里做了纠正，不能只看原理图就生搬硬套�?
4. 右轮编码器方向在软件里做了取反修正�?
5. 初次联调电机和步进时，建议先悬空轮子或脱离机械负载�?

硬件接线详情请配�?[硬件连接与外设映射总表](05_硬件连接与外设映射总表.md) 一起看�?

## 5. 获取工程并打开

### 5.1 打开方式

把整个仓库目录直接作�?`CLion` 工程打开，不要只打开某个子文件夹�?

正确做法�?

1. 打开 `CLion`
2. 选择 `Open`
3. 选择仓库根目�?`Summer_26_zgt6_freertos`
4. 等待 `CLion` 识别 `CMakeLists.txt`

### 5.2 为什么必须打开仓库根目�?

因为下面这些关键文件都在根目录：

1. `CMakeLists.txt`
2. `CMakePresets.json`
3. `Summer_26_zgt6_freertos.ioc`
4. `.mxproject`
5. `STM32F407XX_FLASH.ld`

如果不是从根目录打开，`CLion` 容易只将其识别为普通文件夹，而不是完整的嵌入式工程�?

## 6. 第一次构建前要检查什�?

### 6.1 工具链是否可�?

至少应保证下面这些命令或等价工具存在�?

1. `arm-none-eabi-gcc`
2. `cmake`
3. `ninja`

如果环境使用 `STM32CubeCLT`，这些工具通常可以随其一同安装�?

### 6.2 了解本工程是如何构建�?

本工程使用：

1. `CMakePresets.json` 管理构建预设
2. `cmake/gcc-arm-none-eabi.cmake` 指定 ARM 交叉工具�?
3. `cmake/stm32cubemx/CMakeLists.txt` 收纳 `CubeMX` 生成的底层文�?
4. 根目�?`CMakeLists.txt` 收纳项目 `Code/` 分层代码

简单理解就是：

1. `Core/Drivers/Middlewares` 这些底层�?`CubeMX` 生成并被 CMake 引入�?
2. `Code/` 下的任务、模块、驱动、公共算法由项目维护并被 CMake 引入�?

## 7. 第一次编�?

### 7.1 命令行方�?

在仓库根目录执行�?

```powershell
cmake --preset Debug
cmake --build --preset Debug
```

### 7.2 应生成的主要产物

成功后，最重要的产物通常在：

```text
build/Debug/Summer_26_zgt6_freertos.elf
build/Debug/Summer_26_zgt6_freertos.map
```

### 7.3 如果编译失败，先检查这 5 件事

1. `arm-none-eabi-gcc` 是否存在�?
2. `cmake` 版本是否满足要求�?
3. `ninja` 是否存在�?
4. `STM32F407XX_FLASH.ld` 是否在根目录�?
5. `CMakePresets.json` 是否�?`CLion` 正确识别�?

更详细说明见 [CLion环境配置与CMake构建指南](02_CLion环境配置与CMake构建指南.md)�?

## 8. 第一次烧�?

### 8.1 最基础连接

至少接好下面这些线：

1. `ST-LINK SWDIO`
2. `ST-LINK SWCLK`
3. `GND`
4. `3.3V` 或板子自身供电链�?

### 8.2 下载前建议先做的�?

1. 暂时不要把直流电机和步进轴都带负载上电�?
2. 如果当前只需要先确认程序能运行，应先把高风险执行器部分降到最少�?
3. 先确认板子本身能�?`ST-LINK` 正常识别�?

### 8.3 烧录成功后应该先看什�?

1. `OLED` 是否有显示�?
2. 按键是否能触发黄灯和短鸣反馈�?
3. 程序是否没有立即死机、复位或卡死�?
4. `VOFA` 或串口链路是否能建立�?

## 9. 第一次上电后的推荐联调顺�?

不建议在首次联调时一次性接入全部硬件并同时调试�?

推荐顺序如下�?

1. 先只确认编译成功�?
2. 再确�?`ST-LINK` 能下载�?
3. 再确�?`OLED`、按键、LED、蜂鸣器这些低风险链路�?
4. 再确�?`VOFA` 串口通信�?
5. 再确认电池采样值是否合理�?
6. 再确认灰度传感器输入�?
7. 再确认底盘电机方向、PWM、编码器�?
8. 最后再�?`K230 -> StepperTask -> 双轴步进` 整条链路�?

## 10. 为什么这个工程不能只靠“改几个引脚”就复现

很多新手一看到�?STM32 工程，就以为只要�?`CubeMX` 里改引脚就结束了�?

这个工程不是那种结构�?

它至少有 3 层需要一起对应：

1. `CubeMX` 外设�?
   - 决定哪个引脚属于哪个外设
2. 模块绑定�?
   - 决定哪个模块绑定到哪�?`huart/htim/hadc/port/pin`
3. 任务业务�?
   - 决定谁在什么时候使用这些模�?

例如�?

1. 串口改了，不仅要�?`.ioc`，还要改 `task_init.c` �?`task_stepper.c` 的绑定�?
2. LED 换引脚，不仅要改 `.ioc`，还要改 `task_init.c` �?`mod_led` 绑定表�?
3. 电机方向错了，不一定非改原理图，也可能在绑定表或模块逻辑里修正�?

详细改法�?[CubeMX引脚修改与工程维护指南](04_CubeMX引脚修改与工程维护指�?md)�?

## 11. 新手最容易犯的错误

### 11.1 只改 `main.h`，不�?`.ioc`

这是错误的�?

`main.h` 是生成结果，不是主配置源。正确做法应当是先改 `.ioc`，再重新生成代码�?

### 11.2 只会�?`Core/`，不会看 `Code/`

这也是错误的�?

`Core/` 只负责底层初始化，真正的业务行为主要�?`Code/`�?

### 11.3 直接在任务里�?HAL

这会破坏分层设计，也会让后续维护越来越难�?

### 11.4 不共地就调串�?

尤其是步进驱动和 `K230`，不共地时现象会非常混乱�?

### 11.5 不做阶段化联�?

如果一次性把所有外设全部接入，出问题时会很难判断到底是哪一环出错�?

## 12. “新手复现成功”的判定标准

满足下面这些条件，就可以算复现成功：

1. 能在自己的电脑上成功打开和构建工程�?
2. 能下载到 `STM32F407ZGT6` 板子�?
3. 系统启动后各任务能正常运行�?
4. OLED、按键、LED、蜂鸣器这些基础外设可工作�?
5. 能理解这套工程的分层结构，不会把所有改动都往 `Core/` 里堆�?

接下来建议继续阅读：

1. [CLion环境配置与CMake构建指南](02_CLion环境配置与CMake构建指南.md)
2. [烧录、调试、联调指南](03_烧录_调试_联调指南.md)
3. [系统启动流程与运行逻辑总览](06_系统启动流程与运行逻辑总览.md)

