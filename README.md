# Summer_26_zgt6_freertos 项目总览与文档导�?

本文档体系默认面向外部读者，包括复现者、维护者、评阅者和后续扩展使用者。除特别说明外，文档统一采用“本工程 / 本项�?/ 读�?/ 维护�?/ 目标项目”这类客观表述，不以项目作者本人作为叙述对象�?

## 1. 这是什么工�?

这是一个基�?`STM32F407ZGT6` 的嵌入式分层工程，同时承担两种角色：

1. 毕业设计作品展示工程�?
2. 面向他人复现、学习、维护、继续扩展的通用驱动库示例工程�?

工程当前的核心关键词是：

1. `STM32CubeMX + CMake + CLion + ST-LINK`
2. `FreeRTOS`
3. `Task -> Module -> Driver -> HAL` 分层架构
4. 双电机底盘控�?
5. 12 路灰度循�?
6. K230 自定义串口协�?
7. 双轴闭环步进电机控制
8. OLED 显示、VOFA 调试、电池电压采样、按键交�?

本仓库并非“一次性比赛代码”，而是按可维护、可扩展、可迁移的目标组织的工程骨架�?

## 2. 项目定位

### 2.1 毕设定位

从毕设角度看，这个工程用于展示：

1. 本工程已经形成一套完整的 STM32 嵌入式软件架构�?
2. 这套架构可以把底层硬件访问、业务模块封装、任务调度和上层行为分开�?
3. 工程并非单一功能拼接，而是围绕“可维护、可复用、可继续扩展”的目标组织的�?

### 2.2 复现学习定位

从复现和教学角度看，这个工程的目标是�?

1. 让完全不会的新手也能从零搭环境、打开工程、编译、烧录、调试�?
2. 让后来接手的人知道每一个功能是在哪一层实现的�?
3. 让后续维护者明确在更换引脚、板卡、传感器或电机时，应优先修改哪些位置�?
4. 让后续使用者能够将本工程中的驱动层和模块层抽取出来，复用到其他 STM32 工程中�?

## 3. 当前功能边界

这部分必须先讲清楚，避免读者对工程能力产生误解�?

### 3.1 当前已经完成并在代码中有明确落地的部�?

1. `FreeRTOS` 多任务框架已经建立完成�?
2. 初始化闸门、任务同步、串口互斥这些基础调度机制已经建立完成�?
3. LED、蜂鸣器、激光继电器、按键、OLED、电池采样、灰度传感器都已经纳入分层架构�?
4. 直流电机底盘已经具备�?
   - PWM 输出
   - 方向控制
   - 编码器反�?
   - 直线模式控制
   - 循迹模式控制
5. K230 通信协议为项目自定义协议，并已在模块层完成封装�?
6. 双轴步进控制链路已经建立，且�?K230 误差帧、DCC 运行状态建立了联动关系�?
7. VOFA 调试通道已经建立，可以用于曲线联调�?
8. 工程已经可通过 `CMakePresets` �?`CLion` 中构建�?

### 3.2 当前不应该夸大的部分

本工程可以作�?`2025 电赛` 方向的训练、验证和毕设展示基础，但当前文档必须明确�?

1. 它不是“完整覆盖某个电赛题目的最终提交版”�?
2. 当前最明确、最稳定的能力边界是�?
   - 走直�?
   - 基础循迹
   - 双轴步进控制联调
   - 分层驱动库与模块化架构展�?
3. 任何超出这几个边界的能力，除非后续版本继续补齐并重新验证，否则文档不应虚写�?

## 4. 适合谁阅�?

这套文档主要服务 4 类人�?

1. 完全新手：需要从零搭环境并复现工程�?
2. 课程答辩或毕设评阅者：需要快速理解系统做了什么、亮点是什么、边界是什么�?
3. 后续维护者：需要知道改引脚、改外设、改模块时应当动哪一层�?
4. 希望复用本驱动库的读者：需要知道如何将 `Driver / Module` 抽到新工程中继续使用�?

## 5. 阅读顺序

### 5.1 面向完全新手的阅读顺�?

按下面顺序阅读：

1. [新手从零开始复现工程](docs/01_新手从零开始复现工�?md)
2. [CLion环境配置与CMake构建指南](docs/02_CLion环境配置与CMake构建指南.md)
3. [烧录、调试、联调指南](docs/03_烧录_调试_联调指南.md)
4. [硬件连接与外设映射总表](docs/05_硬件连接与外设映射总表.md)
5. [系统启动流程与运行逻辑总览](docs/06_系统启动流程与运行逻辑总览.md)

### 5.2 面向后续维护者的阅读顺序

按下面顺序阅读：

1. [系统启动流程与运行逻辑总览](docs/06_系统启动流程与运行逻辑总览.md)
2. [CubeMX引脚修改与工程维护指南](docs/04_CubeMX引脚修改与工程维护指�?md)
3. [任务层开发与维护指南](Code/01_Task/任务层开发与维护指南.md)
4. [模块层开发与维护指南](Code/02_Module/模块层开发与维护指南.md)
5. [公共层开发与维护指南](Code/03_Common/公共层开发与维护指南.md)
6. [驱动层开发与维护指南](Code/04_Driver/驱动层开发与维护指南.md)
7. [通用驱动库移植与扩展指南](docs/08_通用驱动库移植与扩展指南.md)
8. [目录分层与归档说明](docs/10_目录分层与归档说明.md)
9. [文档维护与更新规范](docs/09_文档维护与更新规范.md)

### 5.3 面向毕设展示与评阅的阅读顺序

按下面顺序阅读：

1. 本文�?
2. [毕业设计作品说明（归档）](Code/99_Archive/docs/07_毕业设计作品说明.md)
3. [系统启动流程与运行逻辑总览](docs/06_系统启动流程与运行逻辑总览.md)
4. [硬件连接与外设映射总表](docs/05_硬件连接与外设映射总表.md)

## 6. 文档索引

| 文档 | 作用 | 适合�?|
| --- | --- | --- |
| [新手从零开始复现工程](docs/01_新手从零开始复现工�?md) | 说明从安装软件到第一次跑起来的完整过�?| 新手 |
| [CLion环境配置与CMake构建指南](docs/02_CLion环境配置与CMake构建指南.md) | 讲清楚本工程�?`CLion` 里的正确打开方式 | 新手、维护�?|
| [烧录、调试、联调指南](docs/03_烧录_调试_联调指南.md) | 讲怎么下载、调试、看串口、看 VOFA | 新手、维护�?|
| [CubeMX引脚修改与工程维护指南](docs/04_CubeMX引脚修改与工程维护指�?md) | 讲怎么改引脚、改外设、重新生成代码而不破坏分层 | 维护�?|
| [硬件连接与外设映射总表](docs/05_硬件连接与外设映射总表.md) | 汇总所有硬件接口、引脚、注意事�?| 新手、维护�?|
| [系统启动流程与运行逻辑总览](docs/06_系统启动流程与运行逻辑总览.md) | 从上电到任务运行的全链路说明 | 所有人 |
| [毕业设计作品说明（归档）](Code/99_Archive/docs/07_毕业设计作品说明.md) | 保留项目专属说明，不作为当前主干入口 | 答辩、评阅 |
| [目录分层与归档说明](docs/10_目录分层与归档说明.md) | 说明主干层次、归档规则与长期保留资产 | 维护者 |
| [通用驱动库移植与扩展指南](docs/08_通用驱动库移植与扩展指南.md) | 说明这套分层代码怎么复用到别的工�?| 维护者、学习�?|
| [文档维护与更新规范](docs/09_文档维护与更新规范.md) | 规定代码变更后文档该怎么同步更新 | 维护者 |

### 6.1 分层手册索引

1. [任务层开发与维护指南](Code/01_Task/任务层开发与维护指南.md)
2. [模块层开发与维护指南](Code/02_Module/模块层开发与维护指南.md)
3. [公共层开发与维护指南](Code/03_Common/公共层开发与维护指南.md)
4. [驱动层开发与维护指南](Code/04_Driver/驱动层开发与维护指南.md)

## 7. 硬件平台概览

### 7.1 主控平台

1. 主控芯片：`STM32F407ZGT6`
2. 使用方式：`反客科技开发板 + 项目配套扩展 PCB`
3. 工程中使用的主要外设�?
   - `ADC1`
   - `I2C2`
   - `TIM2`
   - `TIM3`
   - `TIM4`
   - `UART4`
   - `UART5`
   - `USART2`
   - `USART3`

### 7.2 外围设备

1. 12 路灰度循迹传感器
2. 轮趣 `MG513X` 直流电机
3. 张大头闭环步进电机驱动一体设�?
4. `K230`
5. OLED
6. 蜂鸣器继电器
7. 激光继电器
8. 3 个按�?

### 7.3 需要特别记住的硬件事实

1. OLED �?`I2C2`�?
2. K230 使用 `UART4`，协议为项目自定义的固定长度串口协议�?
3. X、Y 两个步进轴分别占�?`UART5` �?`USART2`�?
4. VOFA �?`USART3`�?
5. LED 是低电平有效�?
6. 电池采样使用 `ADC1_IN2`�?
7. 左电机方向线在软件映射里做了纠正，不能只看原理图就生搬硬套�?
8. 步进驱动通信时必须和单片机共地�?

更详细的接线、引脚、注意事项，请看 [硬件连接与外设映射总表](docs/05_硬件连接与外设映射总表.md)�?

## 8. 软件架构概览

本工程严格遵守单向依赖：

1. `Task -> Module -> Driver -> HAL`
2. 允许 `Task` �?`Module`
3. 允许 `Module` �?`Driver`
4. 允许 `Driver` �?HAL
5. 不允�?`Task` 直接绕过 `Module` 去碰 `Driver` �?HAL
6. 不允�?`Module` 承担任务调度职责
7. 不允�?`Driver` 写业务状态机

### 8.1 各层作用

1. `Code/01_Task`
   - 负责任务、状态机、时序、同步资源消�?
   - 把多个模块组织成完整业务流程
2. `Code/02_Module`
   - 把多个原子驱动能力封装成设备语义或协议语�?
   - 例如电机模块、按键模块、VOFA 模块、K230 模块、Stepper 模块
3. `Code/03_Common`
   - 纯算法和工具
   - 例如 PID、字符串转换、校验函�?
4. `Code/04_Driver`
   - 最小硬件访问抽�?
   - 例如 GPIO、ADC、PWM、Encoder、UART
5. `Core`
   - �?`CubeMX` 生成的底层初始化代码
   - 例如时钟、GPIO、串口、DMA、RTOS 对象创建

## 9. 任务总览

当前任务�?`Core/Src/freertos.c` 创建�?

| 任务�?| 作用 | 重要�?|
| --- | --- | --- |
| `InitTask` | 完成所有默认模块绑定和初始化，然后释放启动闸门 | 最�?|
| `DccTask` | 底盘状态机唯一写入者，管理 OFF/PREPARE/ON/STOP �?IDLE/STRAIGHT/TRACK | 最�?|
| `StepperTask` | 消费 K230 视觉误差帧，控制双轴步进 | 最�?|
| `KeyTask` | 扫描按键并释放信号量 | 重要 |
| `GpioTask` | 控制 LED、蜂鸣器、激光继电器 | 重要 |
| `OledTask` | 刷新 OLED 页面并显示电压和状�?| 重要 |
| `TestTask` | 预留联调任务 | 次要 |
| `defaultTask` | 保底空转任务 | 次要 |

更详细的周期、优先级、状态流转、任务间协作，请�?[系统启动流程与运行逻辑总览](docs/06_系统启动流程与运行逻辑总览.md)�?

## 10. 30 分钟快速上�?

如果当前只需要先完成构建并确认产物生成，可按下面的最简路径进行�?

### 10.1 软件准备

1. 安装 `CLion`
2. 安装 `STM32CubeMX`
3. 安装 `STM32CubeCLT`
4. 保证 `arm-none-eabi-gcc`、`cmake`、`ninja` 可用
5. 准备 `ST-LINK`

### 10.2 构建命令

```powershell
cmake --preset Debug
cmake --build --preset Debug
```

### 10.3 主要产物

1. `build/Debug/Summer_26_zgt6_freertos.elf`
2. `build/Debug/Summer_26_zgt6_freertos.map`

### 10.4 首次上电检�?

1. 开发板能够�?`ST-LINK` 识别
2. `OLED` 能显示页�?
3. 按键能触发黄灯和蜂鸣器反�?
4. `VOFA` 串口能连�?
5. 底盘和步进链路在未联调前不要盲目上电高速运�?

详细步骤请看 [新手从零开始复现工程](docs/01_新手从零开始复现工�?md)�?

## 11. 仓库结构

```text
Summer_26_zgt6_freertos
├─ Code
�? ├─ 01_Task
�? ├─ 02_Module
�? ├─ 03_Common
�? └─ 04_Driver
├─ Core
├─ Drivers
├─ Middlewares
├─ cmake
├─ build
├─ CMakeLists.txt
├─ CMakePresets.json
├─ Summer_26_zgt6_freertos.ioc
├─ .mxproject
└─ README.md
```

### 11.1 必须掌握�?4 个关键文�?

1. `Summer_26_zgt6_freertos.ioc`
   - `CubeMX` 工程文件
   - 改引脚、改外设通常从这里开�?
2. `Core/Src/freertos.c`
   - 任务、信号量、互斥锁创建入口
3. `Code/01_Task/Src/task_init.c`
   - 模块绑定表和初始化入�?
4. `CMakeLists.txt`
   - 用户层源文件是否参与构建，最终以这里为准

## 12. 为什么这个工程适合继续维护

这个工程之所以具有继续维护价值，并不在于“功能数量多”，而在于它已经具备以下结构性优点：

1. 外设初始化和业务逻辑分离�?
2. 引脚映射和任务逻辑分离�?
3. 模块生命周期统一，很多模块都采用 `ctx_init -> bind -> run -> unbind -> deinit` 风格�?
4. 串口协议模块统一�?`drv_uart + mod_uart_guard + tx_mutex`�?
5. 更换引脚和更换业务逻辑时，不必把整个工程推倒重写�?

## 13. 什么时候必须同步改文档

下面这些改动只要发生，就必须同步更新文档�?

1. `freertos.c` 里的任务、栈、优先级、互斥锁、信号量发生变化�?
2. `.ioc` 中任何一个外设、引脚、DMA、时钟发生变化�?
3. `task_init.c` 中任何绑定表发生变化�?
4. `task_dcc.c`、`task_stepper.c` 中状态机或控制流程发生变化�?
5. `mod_k230`、`mod_stepper`、`mod_vofa` 的协议或绑定方式发生变化�?
6. 直流电机、步进、电池、传感器的控制或换算原理发生变化�?

文档同步规则请看 [文档维护与更新规范](docs/09_文档维护与更新规范.md)。

## 14. 最后提�?

这个工程的一个核心价值，不只是“能够运行”，而是“后续读者仍然能够理解、修改和复现”�?

因此，文档应被视为工程的一部分，而不是工程完成之后顺带补写的说明文字�?

